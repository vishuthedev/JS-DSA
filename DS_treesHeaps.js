/**
 * Heaps trees :- Heaps are another type of tree that have some particular rules. 
 * There are two main types of heaps, MaxHeaps and MinHeaps. 
 * In MaxHeaps, parent nodes are always greater than its children,
 * and in MinHeaps, parent nodes are always smaller than its children.
 */

